const config = {
    mapState: {
        isMoving: false,
        currentZoom: 1,
        tileSize: 0,
        showGrid: true,
        snapToGrid: false,
    },

    controlState: {
        selectedToken: null,
    }

};

export default config;